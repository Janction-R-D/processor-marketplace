import { Divider } from 'antd';
import styles from './index.less';

const JanctionDivider = (props) => {
  return <Divider className={styles['janction-divider']} />;
};

export default JanctionDivider;
